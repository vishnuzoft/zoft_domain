<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Domain Registration Successful</title>
  <style>
    body {
      background-color: #f5f5f5;
      padding: 0;
      margin: 0;
      font-family: sans-serif;
      width: 100%;
      height: 100%;
      overflow: hidden;
    }
    .email-container {
      width: 100%;
      height: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .logo {
      max-height: 100px;
    }
    .content {
      background-color: #ffffff;
      padding: 20px;
      width: 70%;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      text-align: center;
      margin: 20px 0;
    }
    .content h1 {
      font-weight: 300;
      margin-top: 0;
    }
    .content p {
      margin-top: 20px;
    }
    .footer {
      margin-top: 20px;
      text-align: center;
      color: #666;
    }
    .domains-table {
      width: 100%;
      margin-top: 20px;
      border-collapse: collapse;
    }
    .domains-table th, .domains-table td {
      border: 1px solid #ddd;
      padding: 8px;
      text-align: left;
    }
    .domains-table th {
      background-color: #f2f2f2;
    }
  </style>
</head>
<body>
  <div class="email-container">
    <div class="content">
      <!-- Company Logo -->
      <img src="" alt="Company Logo" class="logo" />
      <!-- Domain Registration Success Content -->
      <h1>Domain Registration Successful</h1>
      <p>The following domain has been successfully registered:</p>
      <!-- Domain Details Table -->
      <table class="domains-table">
        <thead>
          <tr>
            <th>Domain</th>
            <th>Expiration Date</th>
            <th>Status</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><%= data.domains.domain %></td>
            <td><%= data.domains.expiration_date ? data.domains.expiration_date.toISOString().split('T')[0] : '' %></td>
            <td><%= data.domains.status %></td>
          </tr>
        </tbody>
      </table>
      <!-- Footer Content -->
      <p class="footer">This is an automated email. Please do not reply to this message.</p>
    </div>
  </div>
</body>
</html>
